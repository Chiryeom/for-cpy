# 💕 乙女游戏角色收藏网站

一个精美的乙女游戏角色展示网站，可以展示你喜欢的游戏角色，包含图片、语音和随机台词功能。

## ✨ 功能特点

- 🎴 精美的角色卡片展示
- 🖼️ 点击查看角色大图
- 🔊 播放角色语音
- 💬 随机生成角色台词
- 📱 响应式设计，支持手机和电脑
- 🌸 粉色乙女风格界面

## 📁 项目结构

```
乙女角色收藏/
├── index.html              # 主页面
├── css/
│   └── style.css          # 样式文件
├── js/
│   ├── characters.js      # 角色数据（需要自己编辑）
│   └── main.js            # 交互逻辑
├── assets/
│   ├── images/            # 角色图片文件夹
│   │   └── placeholder.svg
│   └── voices/            # 角色语音文件夹
└── README.md              # 说明文档
```

## 🚀 快速开始

### 1. 添加角色图片

将角色图片放入 `assets/images/` 文件夹，建议命名为：
- `character1.jpg`
- `character2.jpg`
- 等等...

支持的格式：`.jpg`, `.jpeg`, `.png`, `.webp`

### 2. 添加角色语音（可选）

将语音文件放入 `assets/voices/` 文件夹，建议命名为：
- `character1.mp3`
- `character2.mp3`
- 等等...

支持的格式：`.mp3`, `.wav`, `.ogg`

### 3. 编辑角色数据

打开 `js/characters.js` 文件，修改角色信息：

```javascript
const characters = [
    {
        id: 1,
        name: "角色名字",           // 角色名称
        game: "游戏名称",           // 所属游戏
        cv: "声优名字",             // 配音声优
        image: "assets/images/xxx.jpg",    // 图片路径
        voice: "assets/voices/xxx.mp3",    // 语音路径
        description: "角色简介...",         // 角色描述
        quotes: [                           // 角色台词（用于随机生成）
            "台词1",
            "台词2",
            "台词3"
        ]
    },
    // 可以添加更多角色...
];
```

### 4. 本地预览

直接用浏览器打开 `index.html` 文件即可预览。

> ⚠️ 注意：由于浏览器安全限制，语音播放功能可能需要通过本地服务器访问才能正常工作。

如果需要本地服务器，可以使用以下方法：

**方法1：Python**
```bash
# Python 3
python -m http.server 8080

# 然后访问 http://localhost:8080
```

**方法2：VS Code Live Server 插件**
1. 在 VS Code 中安装 Live Server 插件
2. 右键点击 index.html
3. 选择 "Open with Live Server"

## 🌐 部署到 GitHub Pages

### 步骤1：创建 GitHub 仓库

1. 登录 [GitHub](https://github.com)
2. 点击右上角的 "+" 按钮，选择 "New repository"
3. 填写仓库名称（例如：`my-otome-characters`）
4. 选择 "Public"（公开仓库）
5. 点击 "Create repository"

### 步骤2：上传文件

**方法A：使用 GitHub 网页上传**
1. 在仓库页面点击 "uploading an existing file"
2. 将整个项目文件夹拖拽到上传区域
3. 点击 "Commit changes"

**方法B：使用 Git 命令行**
```bash
# 在项目文件夹中执行
git init
git add .
git commit -m "初次提交"
git branch -M main
git remote add origin https://github.com/你的用户名/仓库名.git
git push -u origin main
```

### 步骤3：启用 GitHub Pages

1. 进入仓库页面
2. 点击 "Settings"（设置）
3. 在左侧菜单找到 "Pages"
4. 在 "Source" 下选择 "main" 分支
5. 点击 "Save"
6. 等待几分钟后，你的网站就可以访问了！

网站地址格式：`https://你的用户名.github.io/仓库名/`

## 📝 自定义提示

### 修改网站标题

编辑 `index.html` 中的 `<title>` 标签和 `<h1>` 标签。

### 修改配色

编辑 `css/style.css` 顶部的 CSS 变量：

```css
:root {
    --primary-pink: #ff6b9d;      /* 主色调 */
    --secondary-pink: #ffa5c3;    /* 次要粉色 */
    --light-pink: #fff0f5;        /* 浅粉色 */
    --gold: #d4af37;              /* 金色 */
    /* ... */
}
```

### 添加更多角色

在 `js/characters.js` 的 `characters` 数组中添加新的角色对象即可。

## ❤️ 版权说明

本项目仅供学习交流使用。角色图片、语音等素材版权归原作者/游戏公司所有。

## 🎮 推荐乙女游戏

- 恋与制作人
- 未定事件簿
- 光与夜之恋
- 时空中的绘旅人
- 无法触碰的掌心
- 遥远时空中
- 薄樱鬼
- 歌之王子殿下

---

**Made with 💕 for otome game lovers**
